# 📚 Link Collector (Колекція посилань)

![Build Status](https://img.shields.io/github/actions/workflow/status/Mariia11212/OOP-3sem/docs.yml?label=Docs%20Build)
![Platform](https://img.shields.io/badge/Platform-Windows_Forms-blue)
![Language](https://img.shields.io/badge/Language-C%23-purple)

**Link Collector** — це програма для зручного зберігання, каталогізації та керування бібліографічними посиланнями (книги, статті, веб-ресурси). Проєкт реалізовано в рамках лабораторної роботи з ООП.

## 🔗 [**ВІДКРИТИ ТЕХНІЧНУ ДОКУМЕНТАЦІЮ (Doxygen)**](https://mariia11212.github.io/OOP-3sem/)
> *Документація генерується автоматично через GitHub Actions при кожному оновленні коду.*

---

## ✅ Виконані вимоги лабораторної роботи

Проєкт повністю відповідає завданню **"Варіант 6 – Колекція посилань"**.

### 1. Функціональні можливості
*   **CRUD операції:** Додавання, перегляд, редагування та видалення посилань.
*   **Категоризація:** Створення та видалення власних контекстів (категорій) для посилань.
*   **Пошук:** "Живий" пошук по назві або автору ресурсу.
*   **Генерація цитат:** Підтримка стандартів:
    *   🇺🇦 ДСТУ 8302:2015
    *   🎓 Harvard Style
    *   📝 BibTeX (для LaTeX)
*   **Експорт:** Збереження списку літератури у зовнішній текстовий файл.

### 2. Технічні вимоги (GUI та Архітектура)
Реалізовано класичний графічний інтерфейс (Windows Forms) без використання автогенерованого коду дизайнера.

*   **Кількість форм:** **4**
    1.  `MainForm` (Головне вікно з таблицею).
    2.  `AddEditForm` (Діалог додавання/редагування).
    3.  `CategoryManagerForm` (Керування категоріями).
    4.  `ExportForm` (Налаштування експорту).
*   **Елементи керування (Controls):** **> 30**
    *   Використано: `Button`, `TextBox`, `Label`, `ComboBox`, `NumericUpDown`, `RadioButton`, `ListBox`, `DataGridView`.
*   **Контейнери:**
    *   `Panel` (для меню), `GroupBox` (для вибору формату), `DataGridView` (для даних).
*   **Обробники подій:** **> 12**
    *   Реалізовано події: `Click`, `TextChanged`, `DoubleClick`, `Load` та інші.
*   **Архітектура:**
    *   Чіткий поділ логіки та інтерфейсу (папки `Models`, `Services`, `Forms`).
    *   Бізнес-логіка винесена в класи `LinkRepository` та `CitationService`.

---

## 🛠 CI/CD та Автоматизація
У проєкті налаштовано процес **Continuous Deployment** через **GitHub Actions**:
1.  Створено Workflow `.github/workflows/docs.yml`.
2.  При кожному пуші в гілку `main` або `master`:
    *   Автоматично встановлюється **Doxygen**.
    *   Генерується HTML-документація на основі коментарів у коді.
    *   Сайт публікується на **GitHub Pages**.


*Виконано студентом ФКНК.*
