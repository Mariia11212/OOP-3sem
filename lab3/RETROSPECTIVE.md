# Ретроспектива використання зовнішніх бібліотек

**Студентка:** Марія Яницька  
**Лабораторна робота:** №3  
**Використані бібліотеки:** RestSharp, Newtonsoft.Json

---

### 1. Які задачі планувалося вирішувати?
Необхідно було реалізувати HTTP-клієнт для надсилання **GET-запитів** до публічного API (CoinGecko) та обробляти отриману відповідь у форматі JSON, перетворюючи її на типи даних C# (`decimal`).

### 2. Чому було обрано саме ці бібліотеки?
*   **RestSharp**: Це найпопулярніша альтернатива стандартному `HttpClient`. Я обрала її, бо вона автоматизує багато рутинних речей (робота з параметрами URL, таймаути) і має більш зрозумілий синтаксис для початківців.
*   **Newtonsoft.Json**: Хоча в .NET є вбудований `System.Text.Json`, бібліотека Newtonsoft є класикою індустрії. Вона дуже гнучка і дозволяє працювати з JSON динамічно (через `JObject`), не створюючи окремих класів для кожної відповіді сервера.

### 3. Наскільки просто було встановити та налаштувати?
Завдяки менеджеру пакетів **NuGet** у Visual Studio процес зайняв менше хвилини. Не потрібно було завантажувати файли вручну, налаштовувати шляхи до бібліотек чи лінкер (як це буває в C++). Достатньо було ввести назву пакету і натиснути "Install".

### 4. Документація та зручність використання
*   **RestSharp**: Документація зрозуміла. Метод `AddParameter` виявився дуже зручним — мені не довелося вручну "клеїти" рядок запиту (наприклад, `?ids=bitcoin&vs_currencies=usd`), бібліотека зробила це сама.
*   **Newtonsoft.Json**: Використання методу `JObject.Parse` дозволило мені звертатися до елементів JSON-дерева як до масиву (наприклад, `json["bitcoin"]["usd"]`), що значно скоротило час написання коду.

### 5. Проблеми, що виникли
Спочатку я забула зробити методи асинхронними (`async`/`await`), через що програма працювала некоректно або блокувала потік. Інтерфейс **RestSharp** підштовхнув мене до правильного рішення, оскільки його сучасні методи за замовчуванням мають суфікс `Async`.

### 6. Позитивні аспекти
Код вийшов дуже компактним. Вся логіка запиту і парсингу займає близько 15-20 рядків. Використання зовнішніх бібліотек дозволило абстрагуватися від низькорівневих деталей (робота з сокетами, формування HTTP-заголовків вручну).

### 7. Висновки
Якби я вирішувала цю задачу засобами "чистого" C#, мені довелося б писати значно більше коду для обробки помилок та ручного формування рядків запиту. Використання перевірених часом Open Source бібліотек значно прискорює розробку та робить код чистішим.

---

### 🔗 Використані ресурси
1.  [RestSharp Documentation](https://restsharp.dev/) — офіційна документація клієнта.
2.  [Newtonsoft.Json Documentation](https://www.newtonsoft.com/json) — приклади роботи з LINQ to JSON.
3.  [CoinGecko API](https://www.coingecko.com/en/api/documentation) — опис формату даних.
